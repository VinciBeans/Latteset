# ============ Pi 编码代理本地状态（运行产物，勿上传） ============
.pi/
# DeepSeek Harness（DSH）会话/状态目录（运行产物，勿上传）
.dsh/

# ============ 依赖 ============
node_modules/
.pnp
.pnp.js
# 沙箱内 npm/pnpm 缓存被拒时的重定向目标（见 docs/troubleshooting.md）
.pnpm-store/

# ============ 构建产物 ============
dist/
dist-ssr/
coverage/
*.local

# pdf.js 资源（构建/开发时由 scripts/copy-pdf-worker.mjs 从 node_modules/pdfjs-dist 自动拷贝，
# 属重生成依赖拷贝，勿入库；dev/build 会先自动生成）
public/pdfjs/

# Rust / Tauri（src-tauri/.gitignore 另有 /target/ 与 /gen/schemas）
src-tauri/target/
src-tauri/gen/

# ============ Latteset 编译中间文件（见 docs/design.md：统一收纳于 tmp/） ============
tmp/

# ============ 日志 ============
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ============ 环境变量与密钥（安全兜底） ============
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx

# ============ 编辑器 / IDE ============
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ============ 操作系统 ============
.DS_Store
Thumbs.db

# ============ Agent 工作手册（局部开发约定，勿上传） ============
AGENTS.md

# ============ 本地测试工程（test_file/projects，含编译产物 tmp/、*.pdf，勿上传） ============
test_file/projects/

# ============ 调研临时产物（语料扫描 JSON / 编译矩阵工作区，勿上传；脚本在 scripts/） ============
test_file/research/

# ============ 端到端测试本地驱动/依赖（勿上传） ============
test_file/e2e/drivers/
.npm-cache/

# ============ LuaJIT 实验构建副本（研究用，含嵌套 .git 与构建产物，勿上传） ============
test_file/luajit-src/

# ============ 本地 TeX 实验件（mylatexformat 提取物 + 工作区 texmf 树，勿上传） ============
test_file/mylatexformat/
test_file/texmf/

# ============ Tectonic 实验件（源码副本 + 下载的安装包，勿上传） ============
test_file/tectonic-src/
test_file/tectonic-install/

# ============ Tectonic 库形态本地构建依赖（vcpkg pin rev 检出 + port 构建产物，数 GB，勿上传） ============
# 用途：本地/CI 构建 tectonic 引擎所需的 C 依赖（graphite2/icu/freetype/fontconfig/harfbuzz）。
# pin 版本见 test_file/tectonic-src/Cargo.toml 的 [package.metadata.vcpkg] rev。
# 复现：git clone https://github.com/microsoft/vcpkg test_file/vcpkg && git -C test_file/vcpkg checkout a62ce77d56ee07513b4b67de1ec2daeaebfae51a
#       && test_file/vcpkg/bootstrap-vcpkg.bat -disableMetrics
test_file/vcpkg/
# 上面那条命令的配套产物：装好的端口（installed/，freetype/harfbuzz/ICU… 数百 MB）、
# 二进制缓存（bincache/）、以及换新版 vcpkg-tool 时的落点（vcpkg-tool-new/）。
test_file/vcpkg-installed/
test_file/vcpkg-bincache/
test_file/vcpkg-tool-new/

# ============ AgentTeams 运行期状态（团队/任务/邮箱，属本地状态勿上传） ============
.agent-teams/

# ============ Tectonic 库形态调研的实验件（实测 crate/夹具/原始证据，勿上传） ============
# 注意：test_file/ 本身未被整体忽略，只有下面这些具名子目录被忽略——别把本行当成通配。
# 需要入库的结论文档请放 docs/research/（见 docs/research/tectonic-lib-evidence.md）。
test_file/research-tectonic-lib/
test_file/t14-evidence/
# `cargo vendor` 的产物（824 MB、650 个包）：只为排查/打补丁临时取回，勿上传。
test_file/vendor/

# ============ 库形态「真编出 PDF」的夹具与缓存（t10 起，数十 MB，勿上传） ============
# bundle 是**目录 bundle**（424 个文件 / 34 MB，含 SHA256SUM=6ffe055852…）：它可从 Tectonic 自己的
# 缓存 `bundles/data/<digest>/` 原样复制出来（那是上游 default_bundle_v33.tar 的解包目录），
# 因此不必入库——真机复核按 docs/tectonic-library-plan.md §6 P2 现场造一个即可。
test_file/tectonic-bundle/
# 产品缓存目录（formats/ 里的 23 MB `.fmt` + bundles/）：一次生成、反复复用，勿上传。
test_file/tectonic-cache/
test_file/tectonic-cli-cache/
# 库形态的真机编译夹具（输入 .tex + 产物 tmp/、*.pdf）。
test_file/tectonic-lib-run/
test_file/t21-verify/
test_file/t24-verify/

# ============ 团队产出的中间 part（正式副本由 T5 落到 docs/research/tectonic-plan-parts/） ============
test_file/tectonic-plan-parts/

# ============ 本地排查小工具（会话转录解压等一次性辅助脚本） ============
test_file/tools/

# ============ Logo 工具临时产物（临时目录 + 预览页，勿上传） ============
*.tmpdir/
__logo_preview*.html

# ============ 真机验证截图（agent 在仓库根目录另存的 PNG/JPG，勿上传） ============
/zoom-*.png
/zoom-*.jpg
/screenshot*.png
/screenshot*.jpg
